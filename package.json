{
  "name": "blissful-infra",
  "version": "0.1.0",
  "description": "Infrastructure that thinks for itself. Ship fast. Break nothing. Learn everything.",
  "keywords": [
    "infrastructure",
    "devops",
    "cli",
    "kubernetes",
    "docker",
    "ci-cd",
    "chaos-engineering",
    "llm",
    "ai"
  ],
  "homepage": "https://github.com/you/blissful-infra#readme",
  "bugs": {
    "url": "https://github.com/you/blissful-infra/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/you/blissful-infra.git"
  },
  "license": "MIT",
  "author": "",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/cli && npm run build -w packages/dashboard",
    "build:shared": "npm run build -w packages/shared",
    "build:cli": "npm run build -w packages/shared && npm run build -w packages/cli",
    "build:dashboard": "npm run build -w packages/shared && npm run build -w packages/dashboard",
    "dev": "./dev.sh",
    "test": "echo 'No tests configured yet'"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
